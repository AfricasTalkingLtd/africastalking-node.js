import { Credentials } from '../../utils/getFullCredentials.types';
import { UploadMediaFile } from './uploadMediaFile.types';
export declare const uploadMediaFile: (credentials: Credentials) => UploadMediaFile;
